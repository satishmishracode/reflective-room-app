# reflective-room-app

Poetry submission app using streamlit.

## Simple Calculator

This repository also includes a small command-line calculator in `calculator.py`.

### Usage

To perform a calculation:

```bash
python calculator.py 2 add 3
```

The example above will output `5.0`.

### Running tests

Unit tests for the calculator can be run with:

```bash
pytest
```
